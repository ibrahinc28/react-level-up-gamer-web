test.skip('test temporal App', () => {});
/*import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

// Mock para react-router-dom para evitar error de módulo no encontrado
jest.mock('react-router-dom', () => ({
  MemoryRouter: ({ children }) => <>{children}</>,
}));

test('renderiza App sin errores y muestra contenidos básicos', () => {
  render(
    <MemoryRouter>
      <App />
    </MemoryRouter>
  );

  // Verifica que el texto del navbar o cualquier componente esperado aparezca
  expect(screen.getByText(/Categorias/i)).toBeInTheDocument();

  // Añade otros expect según tu UI
});*/

